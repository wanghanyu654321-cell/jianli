# Agent Application Engineer Gap Record

JD MASTER 将 R7 定义为 Stretch。当前不生成两页岗位族基线，也不把它作为简历训练中心。

## Existing project material

事实源描述了 Runtime、Tool Calling、RAG、权限/安全、Evidence Governance、Eval 和测试集，但当前工作区没有对应 `wanghanyu654321-cell/-agent` checkout，无法用 `repo-to-resume` 提升为仓库核验实现。

## Evidence gaps

- LangGraph、LlamaIndex、Redis、Milvus、MySQL、Multi-Agent、后端生产服务等没有 FACT_SOURCE_OF_TRUTH 直接证据。
- Hybrid Search、RRF、Runtime Reranker、Formal Query Rewrite、Model Routing 在事实源中明确为未完成/非默认路径。
- 不能把 AI Coding Agent 协作写成独立手写全部代码。

## Future gate

只有补入实际仓库、运行证据和一条真实 JD 后，才重新评估 R7；在此之前只作为学习与证据补强清单。
